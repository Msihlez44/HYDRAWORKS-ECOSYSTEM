export class RateLimiter{private buckets=new Map<string,{count:number;resetAt:number}>();constructor(private readonly limit:number,private readonly windowMs:number){}hit(key:string,now=Date.now()){const current=this.buckets.get(key);if(!current||current.resetAt<=now){this.buckets.set(key,{count:1,resetAt:now+this.windowMs});return{allowed:true,remaining:this.limit-1,resetAt:now+this.windowMs}}current.count++;return{allowed:current.count<=this.limit,remaining:Math.max(0,this.limit-current.count),resetAt:current.resetAt}}}
export function originAllowed(origin:string|undefined,allowedOrigin:string|undefined,method:string){if(['GET','HEAD','OPTIONS'].includes(method)||!origin)return true;if(!allowedOrigin)return process.env.NODE_ENV!=='production';return origin===allowedOrigin}
export function safeExternalUrl(value:string){const url=new URL(value);if(url.protocol!=='https:')throw new Error('Only HTTPS URLs are allowed');return url.toString()}
export function redact(value:Record<string,unknown>){const blocked=/password|token|secret|authorization|cookie/i;return Object.fromEntries(Object.entries(value).map(([key,item])=>[key,blocked.test(key)?'[REDACTED]':item]))}
