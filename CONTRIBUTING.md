# Contributing
Create a focused branch, include tests, run `npm test` and `npm run build`, then open a pull request. Database changes require a Prisma migration. Never commit secrets, production data, uploaded identity documents or generated dependencies.
