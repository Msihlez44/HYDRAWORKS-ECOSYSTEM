import React,{useState}from'react';

const api=async(path:string,options:RequestInit)=>{const response=await fetch(path,{...options,headers:{'Content-Type':'application/json',...(options.headers||{})}});const data=response.status===204?null:await response.json();if(!response.ok)throw new Error(data?.error||'Request failed');return data};

export function Settings({onBack}:{onBack:()=>void}){const[message,setMessage]=useState(''),[error,setError]=useState(''),[saving,setSaving]=useState(false);const submit=async(event:React.FormEvent<HTMLFormElement>)=>{event.preventDefault();setMessage('');setError('');const form=event.currentTarget,data=Object.fromEntries(new FormData(form)),newPassword=String(data.newPassword||''),confirmPassword=String(data.confirmPassword||'');if(newPassword!==confirmPassword){setError('New passwords do not match');return}setSaving(true);try{await api('/api/auth/change-password',{method:'POST',body:JSON.stringify({currentPassword:data.currentPassword,newPassword})});form.reset();setMessage('Password changed. Other signed-in sessions were closed.')}catch(value){setError((value as Error).message)}finally{setSaving(false)}};return <section className="settings"><button className="back" onClick={onBack}>← Overview</button><header><div><p>ACCOUNT SECURITY</p><h1>Settings</h1></div></header><div className="settings-card"><h2>Change password</h2><p>Use at least 10 characters with uppercase, lowercase, and a number.</p><form onSubmit={submit}><label>Current password<input name="currentPassword" type="password" autoComplete="current-password" required/></label><label>New password<input name="newPassword" type="password" autoComplete="new-password" minLength={10} required/></label><label>Confirm new password<input name="confirmPassword" type="password" autoComplete="new-password" minLength={10} required/></label>{error&&<p className="error">{error}</p>}{message&&<p className="success">{message}</p>}<button className="primary" disabled={saving}>{saving?'Saving…':'Change password'}</button></form></div></section>}
