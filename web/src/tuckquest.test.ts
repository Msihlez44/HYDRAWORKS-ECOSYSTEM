import{describe,expect,it}from'vitest';import{assertTransition,calculateDeliveryFee,stockRequestSchema}from'../../src/tuckquest.js';
describe('TUCKQUEST rules',()=>{it('validates multiple items',()=>expect(stockRequestSchema.parse({businessId:'b1',supplierName:'Makro',deliveryAddress:'12 Main Road',items:[{productName:'Rice',quantity:2,estimatedUnitCents:12000},{productName:'Oil',quantity:1,estimatedUnitCents:8000}]}).items).toHaveLength(2));it('prevents skipping approval',()=>expect(()=>assertTransition('SHOPPING','PURCHASED')).toThrow());it('allows delivery lifecycle',()=>{expect(()=>assertTransition('AWAITING_APPROVAL','PURCHASED')).not.toThrow();expect(()=>assertTransition('PURCHASED','DELIVERING')).not.toThrow();expect(()=>assertTransition('ARRIVED','DELIVERED')).not.toThrow()});it('enforces minimum fee',()=>expect(calculateDeliveryFee({baseFare:1000,perKm:100,distanceKm:2,timeRate:10,minutes:5,platformFee:100,minimumFee:2000})).toBe(2000))});
