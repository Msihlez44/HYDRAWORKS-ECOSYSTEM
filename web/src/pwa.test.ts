import{describe,expect,it}from'vitest';import{canRegisterServiceWorker}from'./pwa';describe('PWA',()=>{it('registers only in production web contexts',()=>{expect(canRegisterServiceWorker('production','https:')).toBe(true);expect(canRegisterServiceWorker('development','https:')).toBe(false)});it('supports local production smoke testing',()=>{expect(canRegisterServiceWorker('production','http:')).toBe(true)});});
