import{describe,expect,it}from'vitest';import{canAccessConversation,messageSchema,uniqueParticipants}from'../../src/messaging.js';
describe('Communication',()=>{it('requires message content',()=>expect(()=>messageSchema.parse({attachments:[]})).toThrow());it('accepts safe image metadata',()=>expect(messageSchema.parse({attachments:[{type:'IMAGE',url:'https://files.example/test.png',fileName:'test.png',mimeType:'image/png',sizeBytes:1200}]}).attachments).toHaveLength(1));it('rejects executable attachments',()=>expect(()=>messageSchema.parse({attachments:[{type:'DOCUMENT',url:'https://files.example/a.exe',fileName:'a.exe',mimeType:'application/x-msdownload',sizeBytes:10}]})).toThrow());it('deduplicates participants and enforces access',()=>{const ids=uniqueParticipants(['u1','u2','u1']);expect(ids).toEqual(['u1','u2']);expect(canAccessConversation(ids,'u3')).toBe(false)})});
