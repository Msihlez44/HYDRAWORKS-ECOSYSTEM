import{describe,expect,it}from'vitest';import{originAllowed,RateLimiter,redact,safeExternalUrl}from'../../src/security';describe('security hardening',()=>{it('rate limits repeated authentication attempts',()=>{const limiter=new RateLimiter(2,60000);expect(limiter.hit('ip',0).allowed).toBe(true);expect(limiter.hit('ip',1).allowed).toBe(true);expect(limiter.hit('ip',2).allowed).toBe(false)});it('enforces same-origin state changes',()=>{expect(originAllowed('https://hydra.test','https://hydra.test','POST')).toBe(true);expect(originAllowed('https://evil.test','https://hydra.test','POST')).toBe(false);expect(originAllowed('https://evil.test','https://hydra.test','GET')).toBe(true)});it('permits only HTTPS external references',()=>{expect(safeExternalUrl('https://files.example/doc.pdf')).toContain('https://');expect(()=>safeExternalUrl('javascript:alert(1)')).toThrow()});it('redacts sensitive logging fields',()=>expect(redact({email:'a@b.c',password:'secret',accessToken:'x'})).toEqual({email:'a@b.c',password:'[REDACTED]',accessToken:'[REDACTED]'}))});
