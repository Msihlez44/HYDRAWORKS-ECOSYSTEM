import{createHmac}from'node:crypto';import{describe,expect,it}from'vitest';import{calculateCommission,SandboxPaymentProvider,signedAmount}from'../../src/wallet.js';
describe('HYDRA Wallet',()=>{it('calculates commission snapshot',()=>expect(calculateCommission({grossCents:100000,commissionBasisPoints:1000,paymentFeeCents:250,vatBasisPoints:1500})).toEqual({grossCents:100000,platformCommissionCents:10000,paymentFeeCents:250,vatCents:1500,providerAmountCents:88250,netSettlementCents:88250}));it('calculates ledger direction',()=>{expect(signedAmount('CREDIT',500)).toBe(500);expect(signedAmount('DEBIT',500)).toBe(-500)});it('verifies webhook signatures',()=>{const secret='a-secure-test-secret-1234',provider=new SandboxPaymentProvider(secret),body='{"status":"SUCCEEDED"}',signature=createHmac('sha256',secret).update(body).digest('hex');expect(provider.verifyWebhook(body,signature)).toBe(true);expect(provider.verifyWebhook(body,'bad')).toBe(false)});it('rejects impossible fees',()=>expect(()=>calculateCommission({grossCents:100,commissionBasisPoints:9000,paymentFeeCents:100,vatBasisPoints:1500})).toThrow())});
