# Security Policy
Report vulnerabilities privately to HYDRA WORKS. Do not include credentials or personal information in public issues. Never commit `.env` files. Production deployments must use TLS, a unique 32+ character JWT secret, managed database backups, restricted database credentials and secure cookie settings.
