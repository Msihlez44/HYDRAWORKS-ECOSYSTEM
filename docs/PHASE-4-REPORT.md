# Phase 4 — HYDRA Wallet and Payments

Phase 4 introduces an append-only wallet ledger, sandbox-only provider adapter, signed webhook verification, idempotent payment records, configurable commission rules, settlement snapshots, refunds through reversal entries and driver earnings summaries.

Production payments fail closed until a real provider adapter and credentials are configured. Withdrawals remain unavailable because no payout provider has been selected.
