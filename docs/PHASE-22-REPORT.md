# Phase 22 — Local development package

The root README now covers a reproducible fresh-clone setup, supported runtime, every configured environment variable, local schema creation, the development/production migration distinction, repeatable seed data, development startup, CI-equivalent tests, production builds, backups, and common failures.

Node 22 is pinned for local version managers and matches CI. Package scripts expose both interactive development migrations and non-interactive production migration deployment. No shared or demo administrator password is committed.
